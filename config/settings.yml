user:
  email:
    max_length: 255
  email_regex: !ruby/regexp '/\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i'
  name:
    max_length: 50
  password:
    min_length: 6
    regex: !ruby/regexp /^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[^\w\s])/i
  image:
    image_path: ["image/jpeg", "image/gif", "image/png"]
report:
  column:
    max_length: 255
form:
  create_form:
    num_row: 4
    num_row_comment: 5
reports:
  report:
    format_datetime: "%d/%m/%Y %H:%M"
    format_date: "%d/%m/%Y"
  status:
    waitting: 0
    checked: 1
comment:
  max_length: 500
image:
  default_avatar: "default-avatar.jpg"
  en_image: "en.jpg"
  vi_image: "vi.jpg"
paginate:
  items:
    item_per_page: 10
language_size: 30
rspec:
  default_password: "Vdd@11111111"
  factory_reports_size: 15
  factory_comments_size: 10
email:
  host: "localhost:3000"
  default: "abcdef9@gmail.com"
